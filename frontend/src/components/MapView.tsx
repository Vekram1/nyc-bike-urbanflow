"use client";

export default function MapView() {
  return (
    <div
      style={{
        height: "480px",
        borderRadius: "12px",
        background: "#e9e3d8",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      MapLibre + deck.gl placeholder
    </div>
  );
}
